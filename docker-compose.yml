version: '3.8'

services:
  music-bot:
    build: .
    container_name: discord-music-bot
    env_file: .env
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
